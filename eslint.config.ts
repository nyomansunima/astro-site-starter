import {
  astro,
  combine,
  comments,
  ignores,
  imports,
  javascript,
  jsdoc,
  jsonc,
  markdown,
  node,
  sortPackageJson,
  sortTsconfig,
  toml,
  typescript,
  unicorn,
  yaml,
} from "@antfu/eslint-config"

export default combine(
  ignores(),
  javascript(),
  comments(),
  node(),
  jsdoc(),
  imports(),
  unicorn(),
  typescript(),
  astro(),
  jsonc(),
  yaml(),
  toml(),
  markdown(),
  sortPackageJson(),
  sortTsconfig(),
)
